export interface Config{
  logo?: string,
  icon?: string,
  title?: string,
  banner?: string,
  location_zoom?: number,
  location_lng?: number,
  location_lat?: number,
  map_top?:boolean,
  map_bottom?:boolean,
  logo_width?:number,
  has_hours?:boolean,
  activities_hours_table?:boolean,
}
